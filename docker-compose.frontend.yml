services:
  galaxy-archive-fe:
    image: ghcr.io/${REPO_LOWER}/galaxy-archive-fe:latest
    container_name: galaxy-archive-fe
    restart: always
    env_file:
      - .front.env
    networks:
      - galaxy-network

networks:
  galaxy-network:
    external: true